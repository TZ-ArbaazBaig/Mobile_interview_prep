class AppConstants {
  AppConstants._();

  static const String appName = 'InterviewPrep AI';
  static const String welcomeTagline = 'Master mock interviews with real-time AI feedback.';
  
  // Animation Durations
  static const Duration animShort = Duration(milliseconds: 200);
  static const Duration animMedium = Duration(milliseconds: 350);
  static const Duration animLong = Duration(milliseconds: 500);

  // Error Messages
  static const String genericError = 'An unexpected error occurred. Please try again later.';
  static const String networkError = 'Network error. Please check your internet connection.';
}
